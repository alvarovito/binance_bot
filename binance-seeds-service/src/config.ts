export const SEED_COUNT = 25;
export const INITIAL_SEED_VALUE = 1; // in euros
export const WEIGHT_THRESHOLD = 1; // configurable threshold for filling seeds
export const MIN_PROFIT = 0.01; // minimum profit to consider for orders
export const BINANCE_API_BASE_URL = 'https://api.binance.com/api/v3'; // base URL for Binance API
export const API_CALL_INTERVAL = 1000; // interval for API calls in milliseconds (adjust as needed)